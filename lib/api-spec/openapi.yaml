openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: esim
    description: Live eSIM catalog and ordering
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /esim/plans:
    get:
      operationId: getEsimPlans
      tags: [esim]
      summary: List eSIM plans
      description: Returns live eSIM plans from the connected eSIM Access account.
      parameters:
        - name: locationCode
          in: query
          required: false
          schema:
            type: string
            default: ""
        - name: iccid
          in: query
          required: false
          schema:
            type: string
            default: ""
      responses:
        "200":
          description: Live eSIM plans
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsimPlansResponse"
        "502":
          description: eSIM provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /esim/balance:
    get:
      operationId: getEsimBalance
      tags: [esim]
      summary: Get account balance
      responses:
        "200":
          description: Current account balance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsimBalance"
        "502":
          description: eSIM provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /esim/lookup:
    get:
      operationId: getEsimLookup
      tags: [esim]
      summary: Look up an eSIM by ICCID
      description: Returns only supplier information supported by the eSIM Access response.
      parameters:
        - name: iccid
          in: query
          required: true
          schema:
            type: string
            minLength: 10
      responses:
        "200":
          description: eSIM details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsimLookup"
        "400":
          description: Invalid ICCID or eSIM not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: eSIM provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /esim/topups:
    get:
      operationId: getEsimTopups
      tags: [esim]
      summary: List compatible top-up packages
      parameters:
        - name: iccid
          in: query
          required: true
          schema:
            type: string
            minLength: 10
      responses:
        "200":
          description: Compatible top-up packages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsimPlansResponse"
        "400":
          description: Invalid ICCID
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: eSIM provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      operationId: createEsimTopup
      tags: [esim]
      summary: Submit a top-up to eSIM Access
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EsimTopupInput"
      responses:
        "201":
          description: Top-up accepted for processing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsimTopup"
        "400":
          description: Invalid request or incompatible package
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: eSIM provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /esim/orders:
    post:
      operationId: createEsimOrder
      tags: [esim]
      summary: Purchase an eSIM
      description: Places a live order against the connected eSIM Access account and waits briefly for provisioning details.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EsimOrderInput"
      responses:
        "201":
          description: Purchased eSIM
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsimOrder"
        "400":
          description: Invalid request or insufficient balance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: eSIM provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /esim/orders/{orderNo}:
    get:
      operationId: getEsimOrder
      tags: [esim]
      summary: Get eSIM order status
      parameters:
        - name: orderNo
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: eSIM order details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EsimOrder"
        "404":
          description: Order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: eSIM provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
    EsimPlan:
      type: object
      properties:
        packageCode:
          type: string
        name:
          type: string
        location:
          type: string
        pricePhp:
          type: number
        volumeBytes:
          type: number
        dataGb:
          type: number
        duration:
          type: number
        durationUnit:
          type: string
        supportTopUp:
          type: boolean
        speed:
          type: string
        activeType:
          type: number
      required:
        - packageCode
        - name
        - location
        - pricePhp
        - volumeBytes
        - dataGb
        - duration
        - durationUnit
        - supportTopUp
        - speed
        - activeType
    EsimPlansResponse:
      type: object
      properties:
        plans:
          type: array
          items:
            $ref: "#/components/schemas/EsimPlan"
        regions:
          type: array
          items:
            type: string
        total:
          type: number
      required:
        - plans
        - regions
        - total
    EsimBalance:
      type: object
      properties:
        balanceUsd:
          type: number
      required:
        - balanceUsd
    EsimLookup:
      type: object
      properties:
        iccid:
          type: string
        esimTranNo:
          type: ["string", "null"]
        orderNo:
          type: ["string", "null"]
        transactionId:
          type: ["string", "null"]
        packageCode:
          type: string
        packageName:
          type: string
        totalVolumeBytes:
          type: ["number", "null"]
        usedBytes:
          type: ["number", "null"]
        remainingBytes:
          type: ["number", "null"]
        totalDuration:
          type: ["number", "null"]
        durationUnit:
          type: ["string", "null"]
        expiresAt:
          type: ["string", "null"]
        status:
          type: string
        smdpStatus:
          type: string
        supportTopUp:
          type: boolean
        qrCodeUrl:
          type: ["string", "null"]
        shortUrl:
          type: ["string", "null"]
      required:
        - iccid
        - esimTranNo
        - orderNo
        - transactionId
        - packageCode
        - packageName
        - totalVolumeBytes
        - usedBytes
        - remainingBytes
        - totalDuration
        - durationUnit
        - expiresAt
        - status
        - smdpStatus
        - supportTopUp
        - qrCodeUrl
        - shortUrl
    EsimOrderInput:
      type: object
      properties:
        packageCode:
          type: string
          minLength: 1
        count:
          type: number
          minimum: 1
          maximum: 1
      required:
        - packageCode
    EsimTopupInput:
      type: object
      properties:
        iccid:
          type: string
          minLength: 10
        packageCode:
          type: string
          minLength: 1
      required:
        - iccid
        - packageCode
    EsimTopup:
      type: object
      properties:
        transactionId:
          type: string
        iccid:
          type: string
        packageCode:
          type: string
        packageName:
          type: string
        pricePhp:
          type: number
        status:
          type: string
        message:
          type: string
      required:
        - transactionId
        - iccid
        - packageCode
        - packageName
        - pricePhp
        - status
        - message
    EsimOrder:
      type: object
      properties:
        orderNo:
          type: string
        transactionId:
          type: string
        packageCode:
          type: string
        packageName:
          type: string
        pricePhp:
          type: number
        status:
          type: string
        smdpStatus:
          type: string
        iccid:
          type: ["string", "null"]
        esimTranNo:
          type: ["string", "null"]
        qrCodeUrl:
          type: ["string", "null"]
        shortUrl:
          type: ["string", "null"]
        totalVolumeBytes:
          type: ["number", "null"]
        dataGb:
          type: ["number", "null"]
        totalDuration:
          type: ["number", "null"]
        durationUnit:
          type: ["string", "null"]
        expiresAt:
          type: ["string", "null"]
      required:
        - orderNo
        - transactionId
        - packageCode
        - packageName
        - pricePhp
        - status
        - smdpStatus
        - iccid
        - esimTranNo
        - qrCodeUrl
        - shortUrl
        - totalVolumeBytes
        - dataGb
        - totalDuration
        - durationUnit
        - expiresAt

